# Rust build output
/target

# Compiled binaries copied into platform packages by CI — never commit these
npm/tray-hook-*/tray-hook
npm/tray-hook-*/tray-hook.exe

# Node
node_modules/

# OS noise
.DS_Store
Thumbs.db
desktop.ini